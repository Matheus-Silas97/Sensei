<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.matheussilas97.sensei">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sensei">
        <activity android:name=".view.activity.RankingActivity"></activity>
        <activity android:name=".view.activity.NotesAddActivity" />
        <activity android:name=".view.activity.NotesActivity" />
        <activity android:name=".view.activity.CallActivity" />
        <activity android:name=".view.activity.StudentEditActivity" />
        <activity android:name=".view.activity.ReportsActivity" />
        <activity android:name=".view.activity.StudentsAddActivity" />
        <activity android:name=".view.activity.StudentsDetailsActivity" />
        <activity android:name=".view.activity.ClassAddActivity" />
        <activity android:name=".view.activity.LanguageActivity" />
        <activity android:name=".view.activity.StudentActivity" />
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>